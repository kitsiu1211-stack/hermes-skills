---
name: sales-playbook
description: "飞书产品销售话术与竞品对比分析。触发：用户要求 Deep Grill 产品对比、写销售手册、竞品定位分析、卖点提炼。先拿原始产品能力数据再分析——不猜功能。"
aliases: [deep-grill, competitive-analysis, product-positioning, sales-enablement]
requires:
  bins: [lark-cli, lark-c360]
---

# 飞书产品销售话术 / 竞品分析

## 铁律：先验证，再论断

**所有论点必须可追溯到一个可验证的数据源。** 用户会逐一拆穿经不起推敲的论点——一次被拆穿，后续所有论点的可信度归零。

数据源优先级：
1. **用户提供的内部文档**（one-pager、产品功能表、销售培训材料）——最可靠
2. **官方帮助中心 / 产品页面**——可公开验证
3. **真实的客户会议记录**（旁听字幕、妙记转写）——客户视角，最说服力

**禁止**使用以下来源构建论点：
- 推测"这个产品应该能/不能做 X"
- 行业常识"一般来说这种产品..."
- 竞品公开资料的片面解读

## 标准工作流

### 0. 加载产品能力参考和内部文档索引
**每次执行前，先读取以下两个文件：**
- `references/feishu-ai-capabilities.md` —— 豆包/Aily家族/WorkBuddy已验证能力矩阵
- `references/internal-doc-index.md` —— 已搜到的内部关键文档及其token（额度分布、推广实践、计费说明等），避免重复搜索

### 1. 接收任务 → 先问素材
```
用户要求做 Deep Grill / 竞品分析 / 销售手册
→ 不急着开始分析
→ 先问：有没有内部产品能力对照表 / one-pager？
```

### 2. 验证能力矩阵
- 逐条列出待对比产品的**已知能力**（仅限已验证的）
- 标注每个能力的数据源
- 对未验证的能力显式标注"待确认"

### 3. 找真实差异点
- 不编造差异——不是"功能 X 比 Y 强"，而是"场景 A 只能 X 做到"
- **如果两个产品都能做某件事，就说都能做。不要为了差异化而编造差异。**
- 有效的差异通常来自：生态位置（原生 vs 外部）、用户路径（谁在操作）、数据流（数据在哪处理）

### 4. 构建话术
- 话术必须能从已验证的能力推导出来
- **处理不了的场景要诚实说"做不到"，比硬推更有信用**
- 区分"有效论点"和"会被拆穿的论点"——自觉标注后者

### 5. 输出格式
- 用户要求飞书文档时，先确认格式和存放位置，再执行
- 默认使用表格 + 场景分类的结构

## 销售作战手册文档结构

当用户要求写一份面向客户的销售策略/作战手册文档时，使用以下结构——核心诉求是**能落地**，不是喊口号。触发词：销售手册、作战地图、playbook、售卖策略。

### 文档框架

```
🎯 目标（量化指标 + 时间窗口，如"20个AI企业版客户 + 500万ARR"）
💭 思考题（2-3个核心难题，每个附可执行回答，不是问答对而是作战方案）
📊 产品/场景/商业化矩阵（画板 or 表格）
📈 消耗加速策略（来自内部文档验证过的数据）
💰 销售转化链路（分步骤，有4周落地时间表）
📎 附录：参考来源
```

### 关键原则

1. **目标是骨架，思考题是血肉。** 先定目标，再用思考题倒逼可执行方案。
2. **每个"策略"必须有落地时间表 + 验收标准。** 没有时间表的策略 = 无意义的空白文档。
3. **全员推广路径必须拆到可操作的阶段。** 如"全员开通Aily" → 拆成六阶段：高管先行→项目组建→培训开通→场景驱动→保温运营→深度锁定。每个阶段有角色、动作、验收标准。
4. **来源必须是飞书内部文档。** 用 `lark-cli docs +search` 搜，`lark-cli docs +fetch` 拉全文，链接放入附录。优先查阅 `references/internal-doc-index.md`。
5. **消耗加速三板斧**：扩覆盖（豆包/会议纪要入口）→ 提深度（Aily按人头配额度）→ 建场景（工作流/妙搭锁客）。

### 内部文档搜索优先级

做销售作战文档时，按此顺序搜内部资料：
1. 产品能力 & 计费 → `飞书AI版本对比` + `最新AI功能介绍`
2. 额度消耗逻辑 → `AI额度分布逻辑与配置建议`
3. 推广落地实践 → `AI时代企业引入飞书-上线与运营推广实践`
4. 商业化数据 → QBR数据分享

## 已整理客户案例

- `references/case-anker-ai.md` — 安克创新 AI 落地全记录。旗舰版客户，知识问答 14000+ 次/天。核心弹药："先扩权后上线效果差 5-10 倍"、"CFO 恐慌期是最好的推广期"、"润喉糖指数"。
- `references/ai-package-upsell-playbook.md` — AI 包升级赢单打法（9900→9.9万→29.7万→99万）。核心：用量测算表（真实租户数据）+ 决策人路径 + 「一把到位」话术 + 99万必须触达CEO + 跃迁计划边界 + 蓝星/天波特殊案例。来源：2026-08-26 陈少特赢单复盘旁听。
- `references/paid-ai-workshop-selling.md` — **付费工作坊打单路径 + 三大异议话术**（客户要求不限工具 / 「飞书该免费交付」/「2.5万能请清北教授」），含实战价格锚（5万/天 → 2.5万半天+3000激励）、每组必有产出物的交付设计、可引用案例弹药（同仁堂价格巡检、鹿客竞品情报闭环）。来源：2026-09-17 南区豆包周周碰·趣丸付费工作坊赢单复盘。
- `references/competitor-battlecard-doubao-work.md` — **豆包工作竞品战情卡**（WorkBuddy 三条迭代主线 / 千问办公多人工作台+鸿蒙信创 / 能力差异对照表 / 叙事铁律 / 客户侧实证）。⚠️ 竞品月更，用前确认是否过期。
- `references/zero-coding-case-ammo.md` — **零技术背景也能搭：社区案例弹药**（贺馨·文科生接顺丰 API / Lucian·冶金专业靠飞书 CLI / MONA·妙搭 91 版），专治客户那句「我们没人会开发怎么办」；含活动形式样本（7 分钟倒计时 + 强制讲踩坑）。来源：2026-09-17 飞书 AI 绝活大会 LIVE #9 旁听。

## C360 客户查询坑（lark-c360）

- 输出参数是 `--json`，**不是** `--format json`（后者报 "flag provided but not defined"）
- 宽泛关键词（如公司名带地名 "汉阳"）会命中多家同名/近名公司，还可能报 `size exceed limit` → 加 `--limit 3` 重试
- 结果按 account/company/lead/opportunity/order/renewal 分组，客户主体以用户确认为准（详见 feishu-meeting-listen 的 C360 坑）

## 🆕 豆包工作发布后（2026-08-25 起）产品口径变化

**品牌/命名更新**（涉及对客时一律用新名字，勿用旧名）：
- Aily 智能伙伴 → **豆包工作伙伴**（豆包工作的一个功能）
- 豆包企业版 → **8/31 下架**，由 豆包团队订阅 + 豆包企业订阅（旗舰/尊享）承接
- 豆包工作 = 独立 APP（8/25 上线），三端可用；策略中心合并进尊享版

**完整售卖策略速查**：`references/doubao-work-selling.md`（版本体系/定价/AI用量套餐/30天福利/老客迁移/对客口径，来自 2026-08-25 GTM 专场答疑旁听）

## Aily 分享侧重点（客户验证）

基于和生客户反馈，Aily 演示/分享不要罗列功能，要按两条故事线讲：

### 故事线 1：搭知识库
> "我是怎么把一个部门的知识变成 AI 能懂的"

- 入口在哪里、怎么上传、怎么组织
- 不是一次性搭完，是持续养——知识库越用越聪明
- **客户反馈：这个场景本身就很有吸引力**

### 故事线 2：搭完知识库后干活
> "知识库搭好之后，AI 能帮我做什么"

- 挂到团队智能体 → 群聊里 @ 就能问
- 智能体小队：队长分配任务，专家各司其职
- **客户反馈：这是第二个强场景，衔接自然**

### 演示顺序

```
搭知识库（种地）→ 挂智能体（养牛）→ 小队干活（收割）
```

不要一上来就讲智能体小队——客户还没看到"知识从哪来"就会问"那它怎么知道这些的"。

## 典型产品对比锚点

当对比豆包/Aily/WorkBuddy 等 AI 产品时，这些差异点通常成立（但仍需验证）：

| 维度 | 豆包企业版 | Aily | WorkBuddy |
|------|-----------|------|-----------|
| **生态位置** | 飞书内原生 | 飞书内原生 | 外部，通过 CLI/API 接入 |
| **数据流** | 不离开飞书 | 不离开飞书 | 出飞书进腾讯云 |
| **目标用户** | 全员（零门槛） | IT/业务架构师 | 研发 + 泛办公 |
| **触发方式** | 人主动问 | 事件驱动 + 定时 | 人给指令 + 定时 |
| **桌面操作** | 不支持 | 不支持 | 核心能力 |
| **飞书外系统** | 不支持 | 有限（API） | 200+ MCP |

**注意**：此对照表为通用参考模板。每次分析前必须用最新数据验证每一条。

## 🚨 叙事铁律：不要正面比速度 / 不要正面比功能

**实测会被拆穿的论点，不要写进话术：**
- ❌ 「我们比竞品快」——豆包工作复杂任务（如摄像头巡检）实测 10 分钟以上，**约为 WorkBuddy 的 2-3 倍耗时**。正面比速度刚不过。
- ❌ 逐条列功能差异——大厂之间功能差异很快被拉平，客户当场就能反证。

**正确的叙事结构：讲 end-to-end 闭环，而不是单点能力。**
```
巡检发现问题 → 派单 → 多维表格流转 → 处置 → 闭环追踪
（重点讲前半段，但后半段每一环都要备着）
```
速度/单点能力只是闭环里的一环，把故事放到闭环和「产出能持续长出来」上。

### 🚨 第二条铁律（用户 2026-09-17 亲述）：不要正面比数量

**用户原话提炼**：「WorkBuddy 上其实有非常多的连接器，但要讲质量。关键不在多，再多如果不用，对你来说也没有任何价值和作用。我们要少而精，很多连接器还是能够直接作用于你的业务场景里。」

- ❌ 不比连接器数 / 生态伙伴数 / 集成数——比数量等于替对方开产品发布会，客户听完只记住「他们多」。
- ✅ 换标尺：不谈总量，只谈「**您这条业务线真正要连的就那 2-3 个系统，我们把它做深、跑出结果**」。标尺一换成「你这条流程」，对方的数量优势自动失效。
- ⚠️ **别把这条讲成「我们少也没关系」**——客户听到的是「你们少」。正确姿势是**承认 + 转移标尺**，不是辩解。

### 开场定调的一句话（把对话从对比环节拉进场景环节）

> 「我们不比单点快不快、也不比能连多少个，我们比的是——**您这条业务从发现到闭环，能不能在豆包工作上跑完一遍。**」

两条铁律（速度 / 数量）本质是**同一个动作的两面**：把对话从「产品参数对比」拉回「业务结果对比」。参数差距大厂之间随时被拉平，业务闭环不能。

**客户侧实证（2026-09 国企客户）**：客户全员发过 WorkBuddy 账号，实地调研却发现大部分人没用、甚至不知道有这个产品。
→ **客户不在意用哪个工具，只在意能不能解决他的问题。** 打法：靠痛点理解 + 场景方案 + 案例打动。
→ 实操建议：**提前把 demo/案例在自己的豆包工作里跑一遍存底**，客户现场问「你们到底能不能做」时直接展示产出效果，认可度远高于翻文档。

详见 `references/competitor-battlecard-doubao-work.md`（含 WB / 千问办公最新迭代）与 `references/paid-ai-workshop-selling.md`（付费交付场景下的异议话术）。

## 已踩过的坑

### ❌ 不要做的
- 说"X 需要一个个配 MCP 连接器" → 用户反手证明 CLI 可以直接调 API
- 说"X 没有多 Agent 协作" → 用户反手给出文档证明有 SubAgent
- 说"X 没有定时任务" → 用户截图证明有
- 说"X 需要持续维护" → 用户指出 CLI 自动更新
- 说"X 体验不如 Y" → 用户指出 Bot 桥接可以同在飞书内
- 浏览器搜飞书公开页面 → 大部分时间 404 或找不到，直接走 CLI 搜内部文档

### ✅ 应该做的
- 不确定时直接说"我需要验证"
- 论点被拆穿时直接承认，不硬撑
- 所有功能对比从已验证的能力矩阵出发
- **先用 `lark-cli docs +search` 找内部文档，再用 `lark-cli docs +fetch` 拉全文** — 不要用浏览器，不要猜功能

### lark-cli 命令常见坑

- `docs +fetch` 用 `--doc`，不是 `--token`
- `docs +search` 用 `--page-size`，不是 `--limit`
- `docs +update --content @file.md` 要求相对路径。绝对值（如 `/tmp/foo.md`）会报错。改用 `cat /tmp/foo.md | lark-cli docs +update ... --content -` 通过 stdin 传入
- `docs +update --command overwrite` 会清空文档重新写入；画板块需要重新创建（旧的 whiteboard token 会失效）

## 内部文档搜索技巧

### lark-cli docs +search 的正确用法

**字段名是 `results`，不是 `items`！** 这是最常见的 bug —— 搜到了但提取代码写错字段名导致误报 0 结果。

```bash
# 正确提取
lark-cli docs +search --query "豆包" --as user --page-size 10 | \
  python3 -c "
import json,sys
d=json.load(sys.stdin)
for r in d.get('data',{}).get('results',[]):  # ← results, 不是 items
    meta=r.get('result_meta',{})
    print(meta.get('title_highlighted','?'))
    print(meta.get('url','?'))
    print(r.get('summary_highlighted','')[:120])
"
```

响应结构关键字段：
- `data.results[]` — 搜索结果列表
- `result_meta.url` — 文档链接
- `result_meta.token` — doc token，可直接用于 `docs +fetch`
- `result_meta.title_highlighted` — 高亮标题
- `summary_highlighted` — 高亮摘要
- `result_meta.entity_type` — "DOC" 或 "WIKI"

### lark-cli minutes +search 搜妙记（竞品/赢单情报的富矿）

竞品动态、赢单复盘、政策宣贯往往只存在于会议妙记里，文档搜不到。妙记搜索字段路径与 docs 不同：

```bash
lark-cli minutes +search --query "豆包工作" --as user | python3 -c "
import sys,json,re
d=json.load(sys.stdin)
for it in d['data']['items']:
    title=re.sub('<[^>]+>','',it['display_info']).split('\n')[0]  # display_info 是带 <h>/<b> 高亮标签的富文本
    print(title,'|',it['meta_data']['app_link'])                   # 链接在 meta_data.app_link
    # it['token'] 可再用于妙记详情/转写读取
"
```
- 字段是 `data.items[]`（不是 docs 的 `results`），标题在 `display_info`，**必须 strip HTML 标签**，末尾带所有者/开始时间/时长。
- `has_more` 常为 true，一次只返回前若干条 → 换更具体的关键词再搜。
- 🚨 **覆盖盲区**：不是所有会议都生成你可访问的妙记（找不到某场发布会本体的妙记是常态）。找不到就**如实说找不到**，只给确实查到的相关妙记链接，**绝不推测或拼凑链接**——这是「先验证再论断」铁律在检索侧的同一条要求。

### lark-cli docs +fetch 读取文档

```bash
lark-cli docs +fetch --doc <token_or_url> --as user | \
  python3 -c "
import json,sys,re,html
d=json.load(sys.stdin)
content=d.get('data',{}).get('document',{}).get('content','')
text=html.unescape(re.sub(r'<[^>]+>',' ',content))
print(text[:5000])
"
```

### 调试搜索的步骤
1. 先用通用词（"飞书"、"AI"）测试搜索是否工作 → 确认返回总量
2. 如果返回 0，检查提取代码的字段名（`results` vs `items`）
3. 如果返回有数据但特定关键词搜不到，换同义词或拆词重试
4. 搜到文档后先读标题和摘要判断相关性，再 fetch 全文
